#include "main.h"
/**
 * add - adds two integers and returns the result
 * @a: int to be added to b
 * @b: int to be added to a
 * Return: display the sum of two number
 */
int add(int a, int b)

{
	int c;

	c = a + b;
	return (c);
}
