#include "main.h"
/**
 * mul - function that multiplies two integers
 * @a: to accept integer
 * @b: to accept integer
 * Return: multipcation of a and b
 */
int mul(int a, int b)
{
	return (a * b);
}
