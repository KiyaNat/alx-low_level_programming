#include <stdlib.h>
#include<stdio.h>
/**
 * main - prints alpha in lower case
 * Return: Always 0
 */
int main(void)

{
	char lac;

	for (lac = 'a'; lac <= 'z'; lac++)
	{
		putchar(lac);
	}
	putchar('\n');
	return (0);
}
